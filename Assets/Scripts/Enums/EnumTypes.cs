namespace EnumTypes
{
    public enum EggTypes
    {
        None,
        Hatching,
        Sleep,
    }

    public enum GameFlowState
    {
        Init,
        Loading,
        Main,
        Setting,
    }

    public enum GameCurrentState
    {
        Play,
        Pause,
        Quit,
    }
}